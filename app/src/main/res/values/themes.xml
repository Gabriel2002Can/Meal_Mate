<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash screen theme -->
    <style name="SplashTheme" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Background color -->
        <item name="android:windowBackground">@color/splash_screen_background</item>
        <!-- Logo -->
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_splash_screen_logo</item>
    </style>

    <!-- Main app theme -->
    <style name="Theme.MealMate" parent="android:Theme.Material.Light.NoActionBar"></style>
</resources>